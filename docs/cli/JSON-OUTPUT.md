# JSON Output Reference

This is a lightweight index for JSON output behavior in the Steroids CLI.

- Standard JSON envelope and error codes: see [CLI API](./API.md).
- Contract guarantees (flags, exit codes, parseable errors): see [CLI Contract](./contract/CLI_CONTRACT.md).

